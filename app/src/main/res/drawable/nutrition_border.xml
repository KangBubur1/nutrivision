<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent"/> <!-- Warna isi transparent -->
    <stroke android:width="1dp" android:color="@color/black"/> <!-- Warna border -->
    <corners android:radius="4dp"/> <!-- Radius sudut, jika diinginkan -->
</shape>
